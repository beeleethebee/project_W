@font-face {
    font-family: "Mikela";
    src: url(<%= asset_path('MikelaRegular.ttf') %>) format('truetype');

    font-family: "WorkSans";
        src: url(<%= asset_path('WorkSans-Regular.ttf') %>) format('truetype');
        src: url(<%= asset_path('WorkSans-Medium.ttf') %>) format('truetype');
        src: url(<%= asset_path('WorkSans-Semibold.ttf') %>) format('truetype');
}